from distutils.errors import LinkError


t = (1, 3, 5, 9)
# print(t[0])
#A tuple is an immutable data type in Python
# a = () denotes an empty tuple 
# a = (1,) denotes a single element tuple. Needs a comma.
# a = (1, 7, 2) denotes a tuple with more than one element
# Once defined a tuple element cannot be altered or changed or manipulated.
# When printing the elements of tuple use [] 
print(t[0])
